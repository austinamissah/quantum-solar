# Decisions where an easier path was available

Nine forks in this project's record where a cheaper or more flattering option
existed and I took the other one. The "alternative rejected" column is what makes
these decisions rather than events. Predictions and their outcomes are in
[`PREDICTIONS.md`](PREDICTIONS.md).

| date | decision | alternative rejected | what it cost | link |
|---|---|---|---|---|
| 2026-08-02 | Kept instance seed 1 as the optimizer study's primary, designated before any arm ran | Report instance 3, where four arms clear 10 of 10, or instance 2, where the best arm clears 9 of 10 | A negative headline: all 12 arm × α combinations fail on the primary, and no hardware candidate followed from the study | [slack-free-encoding.md](results/slack-free-encoding.md) |
| 2026-08-03 | Retracted "device drift cannot explain this, because drift would have moved both circuits and one of them did not" | Leave the argument in place; the conclusion resting on it did not change when the argument fell | Drift turned out to be real and 43% of the shift, and the first run's write-up now carries a retraction of its own reasoning rather than a clean record | [hardware-run-encoding-replication.md](results/hardware-run-encoding-replication.md) |
| 2026-08-03 | Ran a 10-replicate spread measurement whose plan states in advance that no outcome can strengthen the headline | Publish the two positive gap measurements and leave the device-variance term unmeasured, which is what both prior bootstraps had already done | 48 QPU-seconds on a measurement that could only weaken the claim: σ_device is 70% of the replicate spread, every published single-run interval was too narrow, and run 3 alone no longer excludes zero | [hardware-run-spread.md](plans/hardware-run-spread.md) |
| 2026-08-04 | Dropped the T=6 rung from the α\* and censoring sweeps: the plan excludes it on cost, and the metric had read exactly zero in all nine cells at both T=5 and T=6 | Run the roughly 10-hour sweep and publish nine more zeros as the continuation of a scaling trend | A gap in the scaling comparison: `qaoa_scaling.csv` reaches T=6 while both α\* sweeps stop at T=5, so the weight comparison has no largest-size rung | [eval-censoring.md](plans/eval-censoring.md) |
| 2026-08-04 | Kept the registered censoring classifier, `evals == 1000`, when a more inclusive one was defensible | Reclassify on "did the cell use more budget when offered", which drops the median shift to 8.0% against a 10% threshold and lets every α\* mass value stand as measured | The verdict now carries a label saying it is a judgment made after seeing the data, and every α\* mass in the encoding write-up is published as a lower bound rather than a value | [eval-censoring.md](results/eval-censoring.md) |
| 2026-08-05 | Reopened a gate that had already been closed and published, on an axis the original ladder never varied | Let the qualification added the day before stand and leave the closure unexamined | 120 runs and no new finding: the gate closed again, CONFIRMED-CLOSED, and what the reopening produced was a retraction of the over-correction that prompted it | [optimizer-budget-study.md](plans/optimizer-budget-study.md) |
| 2026-08-07 | Declined the hardware encoding × weight 2×2 because the `cp3 @ default` cell is not well defined | Pin a tuning seed, which makes the cell reproducible without making it defined, and submit a clean-looking 2×2 | The encoding claim rests on a single weight, α=0.021; the interaction it was designed to measure is unmeasured on hardware | [slack-free-encoding.md](results/slack-free-encoding.md) |
| 2026-08-07 | Re-derived that refusal the same day, after both of its stated reasons were measured and found too strong | Leave the decision resting on retracted support, the outcome being unchanged either way | The surviving argument is weaker than either original: only that the weight moves `cp3`'s circuit about 4× less than `exact`'s, recorded in place beside both failed versions | [slack-free-encoding.md](results/slack-free-encoding.md) |
| 2026-08-26 | Narrowed the leg-1 novelty claim on prior art found in a survey's reference list an hour and a half after v1.0.0 was tagged | Keep the wider claim, already archived under a version DOI | The qubit reduction, 117 → 52, is no longer claimed as new; what remains is the containment property alone, and the v1.0.0 snapshot still carries the wider version | [LESSONS.md](LESSONS.md) |
